table:
  name: vote
  schema: public
object_relationships:
  - name: member
    using:
      foreign_key_constraint_on: account
select_permissions:
  - role: client
    permission:
      columns:
        - flag
        - account
        - weight
        - producers
      filter: {}
