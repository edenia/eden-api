table:
  name: vote
  schema: public
object_relationships:
  - name: member
    using:
      foreign_key_constraint_on: account
select_permissions:
  - role: guest
    permission:
      columns:
        - account
        - weight
        - producers
      filter: {}
      allow_aggregations: true
