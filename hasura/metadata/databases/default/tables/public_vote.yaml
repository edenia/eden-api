table:
  name: vote
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - account
        - weight
        - producers
      filter: {}
      allow_aggregations: true
