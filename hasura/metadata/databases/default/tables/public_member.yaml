table:
  name: member
  schema: public
object_relationships:
  - name: vote
    using:
      manual_configuration:
        column_mapping:
          account: account
        insertion_order: null
        remote_table:
          name: vote
          schema: public
select_permissions:
  - role: client
    permission:
      columns:
        - account
        - encryption_key
        - name
        - representative
        - election_participation_status
        - election_rank
        - nft_template_id
        - status
        - profile
      filter: {}
